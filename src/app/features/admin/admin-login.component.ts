import { Component } from '@angular/core'; import { FormBuilder, Validators } from '@angular/forms'; import { Router } from '@angular/router'; import { MockAuthService } from '../../core/services/mock-auth.service';
@Component({selector:'app-admin-login',template:`<section class="admin-login"><form [formGroup]="form" (ngSubmit)="login()"><div class="brand-mark">ॐ</div><h2>Admin demo</h2><p>Use <b>admin@rrdp.demo</b> / <b>demo123</b></p><input class="form-control mb-3" placeholder="Email" formControlName="email"><input type="password" class="form-control mb-3" placeholder="Password" formControlName="password"><p class="text-danger" *ngIf="error">Invalid demo credentials.</p><button class="btn btn-maroon w-100" [disabled]="form.invalid">Sign in</button><a routerLink="/" class="d-block text-center mt-3">Back to website</a></form></section>`}) export class AdminLoginComponent{error=false;form=this.fb.group({email:['admin@rrdp.demo',[Validators.required,Validators.email]],password:['demo123',Validators.required]});constructor(private fb:FormBuilder,private auth:MockAuthService,private router:Router){}login():void{const v=this.form.getRawValue();if(this.auth.login(v.email||'',v.password||''))this.router.navigate(['/admin']);else this.error=true;}}
